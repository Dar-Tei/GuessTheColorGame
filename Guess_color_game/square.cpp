// #include "square.h"

// using namespace std;

// void Square::GetColorSquare(int r, int g, int b) {
//     squareColor = "rgb(" + to_string(r) + ", " + to_string(g) + ", " + to_string(b) + ")";
// }

// void Square::GetSizeSquare(int size) {
//     squareSize = size;
// }

// void Square::SetColorSquare(int r, int g, int b) {
//     // Set color from RGB values
//     squareColor = "rgb(" + to_string(r) + ", " + to_string(g) + ", " + to_string(b) + ")";
// }

// void Square::SetSizeSquare(int size) {
//     squareSize = size;
// }
